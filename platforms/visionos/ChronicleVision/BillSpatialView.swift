import SwiftUI
import RealityKit

/// A 3D bill list that arranges bill cards in a spatial window,
/// allowing the user to browse and interact with bills in space.
struct BillSpatialView: View {
    var body: some View {
        Text("BillSpatialView — spatial bill list")
            .font(.headline)
    }
}
