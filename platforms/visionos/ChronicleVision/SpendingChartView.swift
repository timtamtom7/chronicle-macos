import SwiftUI
import RealityKit

/// An immersive 3D spending chart that visualizes monthly expenses
/// as a volumetric bar chart or radial ring in the user's space.
struct SpendingChartView: View {
    var body: some View {
        Text("SpendingChartView — immersive 3D chart")
            .font(.headline)
    }
}
