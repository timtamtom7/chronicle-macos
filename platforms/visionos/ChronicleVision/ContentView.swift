import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Chronicle for visionOS")
                .font(.title)
            Text("Spatial bill tracking — coming soon")
        }
    }
}
